## Aplicación que da el clima actual de una ciudad

Para ello consume un par de API's

Recordar ejecutar ``` npm install ``` para bajarse las librerías necesarias

### Ejemplo:
```
node app -d "New York"
```